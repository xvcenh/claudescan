# claudescan — Agent Configuration

## Tech Stack

- **Language:** Python
- **Other:** Markdown, TOML
- **Frameworks:** Python
- **Build:** pip → `pip install -e .`

## Commands

| Task | Command |
|------|---------|
| Build | `pip install -e .` |
| Test | `run tests in 'tests/'` |

## Rules

- Write tests for new features
- Follow existing code style
- Use type hints on all new functions
- Import order: stdlib → third-party → local
